import { createUseStyles } from "react-jss";

export default createUseStyles({
  updateForm: {
    display: "grid",
    alignItems: "center",
    justifyContent: "center",
  },

  formGroup: {
    marginTop: "20px",
  },

  skills: {
    margin: "5px 5px",
  },
});
